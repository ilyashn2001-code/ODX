[README.md](https://github.com/user-attachments/files/26462097/README.md)
# Центр геоаналитических инструментов

Обновлённый HTML/JS-прототип для GitHub Pages.

## Что изменено
- поддерживаются два набора данных: **ОГХ** и **КРР**
- набор **«Мой район»** удалён
- набор **«СОК»** переименован в **«КРР»**
- в таблице результата выводятся строки и атрибуты из `Результат анализа.csv`
- на карте отображается WKT-геометрия из столбца `Геометрия неблагоустроенной территории`
- приоритизация влияет на порядок строк результата и цвет объектов на карте

## Важно
- во всех CSV используется разделитель `;`
- в `index.html` добавлен запасной обработчик кнопок шагов, чтобы переходы по шагам не ломались

## Файлы
- `index.html` — разметка
- `styles.css` — стили
- `app.js` — логика приложения
- `data_fixed.js` — данные, собранные из CSV
